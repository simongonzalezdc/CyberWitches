# Multi-model adversarial synthesis — critical path

**Date:** 2026-07-27  
**Base game tip:** Forgejo `main` @ top-15 merged (`be1bb84`)  
**Models attempted:** GLM-5.2 (`claude-glm52`), GPT/Codex-Sol (`codex exec` / dispatch), Kilo free (Opus-proxy failed), MiniMax-M3 (`gjc`), Grok-4.5 (this session)

| Panel | Status | Signal quality |
|-------|--------|----------------|
| **GPT-5.6 Sol (medium)** | Full answer (`02-gpt56-sol-medium.md`) | High — hard gates, first-arc focus, parallel DAG W0–W4 |
| **MiniMax-M3** | Full answer | High — tier event + share pipeline + kill list |
| **Grok-4.5** | Full answer | High — force-multiplier event spine + lanes |
| **Codex-Sol raw** | Session log only (`02b-codex.md`) | Low — incomplete |
| **GLM-5.2** | Empty/timeout this run (`01-glm52.md`) | Unavailable this pass |
| **Kilo/Opus** | CLI failed (`03-kilo-opus.md`) | Unavailable (free lane) |

## Sol medium alignment
Sol emphasizes: **hard gates before content** (trust/comprehension/pacing/differentiation/share); compress first arc to prestige; lab/meditation/share trail; kill second prestige and gacha. Strongly consistent with MiniMax+Grok on W0 truth machinery and anti-slop kill list; more conservative on meditation (one polished session only).

## Consensus North Star (O1)

**Agree (MiniMax + Grok):**  
Ideal mush becomes sharp when **tier transition = primary scoreboard moment**. Strategy optimizes **transitions per retained player** + **shareability of heal**, not abstract “ethical retention” as a moat.

**North Star (locked for map):**  
Ship Hex Compiler so every design-tier advance is an **observable, audible, capturable** system-recovery event that paces the economy—and so the first session forces competence (cast→craft→automation→near-heal) without slop mechanics.

## Consensus critical path (merged)

### Parallel DAG (strategic order)

```
W0  FOUNDATION (all parallel) ─────────────────────────────
    A. E2E required in CI (smoke + progression-tier)     [S/H]
    B. Mult-cache correctness unit (buff/meditation)     [S/H]
    C. Dist/prod smoke script                            [S/M]
    D. Unlock-ratio CI bound (already ~true; harden)     [S/M]

W1  GOAL + EVENT SPINE (parallel after W0 starts) ───────
    E. Always-on goal stack post-TutorialSystem          [M/H]
    F. First-class onTierAdvance event (GameState/UI)    [M/H]
    G. Operator funnel events (tier/share/save only)     [S/M]

W2  HEAL PAYOFF (needs F) — parallel tracks ─────────────
    H. Heal moment package: visual + SYSTEM_LOG + stinger[M/H]
    I. First-session pacing fix to time-to-automation    [M/H]
    J. Meditation Δ mult player feedback                 [S/H]
    K. Save outcome toasts already partly done—close gaps[S/H]

W3  SHARE + RETURN (needs H) — parallel ─────────────────
    L. Capture/share PNG (sanitized; no full save URL)   [M/H]
    M. Landing before/after + thesis line                [S/H]
    N. Prestige ceremony + post-prestige goal stack      [M/H]

W4  DISTRIBUTION (ops + light eng) ──────────────────────
    O. Creator demo seed + 15s heal GIF                  [S/H]
    P. r/incremental_games post + one creator brief      [S/H]
    Q. 30-day pivot: if public shares < threshold, stop virality spend → retention craft only
```

### Serial spine (cannot cheat)

`W0.A` → trust for everything  
`W1.F` → `W2.H` → `W3.L/M` (event → moment → share)  
`W1.E` → `W2.I` / `W3.N` (goals pace session + prestige)

### Maximum parallelization (agent lanes)

| Lane | Owns | Wave |
|------|------|------|
| **Lane CI** | A, C, D | W0 |
| **Lane Economy/UX** | E, I, N | W1–W3 |
| **Lane Heal/Event** | F, H, L, M | W1–W3 |
| **Lane Loop polish** | B, J, K | W0–W2 |
| **Lane Growth** | O, P, Q | W4 after H |

3–4 agents max; do **not** parallelize F and H on conflicting UIManager files without merge discipline.

## Kill list (90 days) — multi-model agree

- Third currency / gacha / pay-power  
- Guilds/leaderboards  
- CSS framework migration  
- Full GameState/audio rewrite  
- Resurrect questSystem as dual HUD  
- Steam before D1 known  
- Streak-hostage retention  
- Analytics sprawl beyond scoped events  

## 2-week max-value plan (if only one agent)

1. A E2E CI  
2. B mult cache  
3. E goal stack  
4. F tier event  
5. H heal moment  
6. J meditation Δ  
7. L share PNG (sanitized)  
8. M landing  

Skip: recap player, meditation restructure, quest revive.

## 90-day plan

2-week + N prestige ceremony + I session timing + O/P distribution + Q pivot gate + optional meditation framing (not full re-arch) if D1 soft.

## Adversarial risks (merged)

1. Heal not actually good on video → kill share work early.  
2. E2E flake destroys main → flake budget / quarantine.  
3. Share leaks save data → sanitize artifacts.  
4. Niche does not share → pivot trigger (MiniMax: &lt;100 public shares / 30d).  
5. Scope greed reopens god-object rewrite.

## Value packing principle

**One event spine (tier advance)** multiplies: audio, share, telemetry, goals, landing.  
Prefer that force-multiplier over ten independent features.
