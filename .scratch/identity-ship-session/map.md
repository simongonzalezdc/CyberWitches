## Destination

Ship Hex Compiler this session as a polished, identity-locked product: player-facing truth matches live systems, hybrid compiler/occult hierarchy is enforced, Tier 0 progressive restoration is a hard contract, and in-scope audit trust fixes land and verify before session end.

## Notes

- Domain: Hex Compiler / CyberWitches idle game (vanilla JS).
- Cadence: single session only — multi-session wayfinder queue cancelled by operator.
- Skills after map: ralplan consensus, brainstorm SPEC, implement + verify.
- Standing preferences locked:
  - Fantasy: hybrid with hierarchy (chrome = compiler; lore = occult content).
  - Currency: Arcane Bits (AB), Eldritch Keys (EK). Kill SE / Aether Bits / Arcane Bytes as living names.
  - Tier 0: hard contract (first paint broken/incomplete until thresholds unlock polish).
  - First session: repair-first + one short compile goal; no brand-new systems.
  - Meditation: post-prestige only; fix save/load + stuck detection.
  - Scope: maximize everything this session (truth + first-session feel + high-value P1s).
- Human-only: rotate/revoke GitHub PAT on `upstream-github` remote.
- Evidence: `artifacts/adversarial-audit-2026-07-26/MASTER_ADVERSARIAL_AUDIT.md`.

## Decisions so far

- Single-session ship (not multi-session decision tickets).
- Product fantasy = hybrid with hierarchy.
- Currency = Arcane Bits (AB) + Eldritch Keys (EK).
- Tier 0 progressive restoration = hard product contract.
- First-session posture = repair-first + one short goal.
- Meditation = post-prestige only (fix integrity).
- Repair scope = everything practical this session.

## Not yet specified

- (cleared) Compile goal pinned: TutorialSystem + fixed copy in SPEC §7.9.
- Tier 0 severity details: minimal shell in Batch 1; full severity in Batch 5 per SPEC §7.8.

## Out of scope

- Multi-session wayfinder ticket cadence (operator rejected).
- Token rotation on GitHub (human action required).
- Full god-object rewrite of audioSystem unless time remains after P0/P1 player fixes.


## Destination reached

2026-07-27: Identity-lock + published tickets shipped (PR #6, #7). Superseded by map **Close open loops** at `.scratch/close-open-loops/map.md`.
