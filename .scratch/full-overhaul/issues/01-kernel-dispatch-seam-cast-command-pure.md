# 01 — Kernel dispatch seam (cast command pure)

**What to build:** A pure Kernel accepts a cast command and returns updated resources/progress without touching the DOM.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] Cast command is pure and unit-tested with fixed seed
- [ ] No DOM import in Kernel module
- [ ] Domain event for cast is emitted

## Parent

Restoration Kernel full overhaul — `.scratch/full-overhaul/PRD.md`

## Notes

Holistic overhaul work package. Waves deliver the same destination; do not shrink vision to this ticket alone.
