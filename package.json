{
  "name": "hex-compiler",
  "version": "1.1.0",
  "description": "Magic is fading. You're a Hex Compiler\u2014translating magical hexes into hexadecimal code. An idle game about compiling spells into data and building digital preservation chambers.",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "dev": "npx http-server . -p 3000 -o",
    "start": "npx http-server . -p 3000 -o",
    "build": "node build.js",
    "build:prod": "node build.js --production",
    "preview": "npx http-server dist -p 3000 -o",
    "package": "bash package-for-friend.sh || ./package-for-friend.sh",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules jest --coverage --runInBand",
    "test:e2e": "playwright test",
    "test:operator": "playwright test e2e/operator-sentry.spec.js",
    "test:visual": "playwright test e2e/visual-a11y.spec.js",
    "optimize:images": "node optimize-backgrounds.js",
    "lint": "eslint .",
    "lint:color-debt": "node scripts/lint-color-debt.js",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "format": "prettier --write \"js/**/*.js\" \"*.{html,css,md}\"",
    "docs": "jsdoc js -d docs -c jsdoc.json",
    "size-check": "node -e \"const fs=require('fs'),p=require('path'),d='dist/js';const crit=fs.statSync(p.join(d,'game.bundle.js')).size;const all=fs.readdirSync(d).filter(f=>f.endsWith('.js')).reduce((s,f)=>s+fs.statSync(p.join(d,f)).size,0);const ck=(crit/1024).toFixed(1),ak=(all/1024).toFixed(1);console.log('Critical bundle: '+ck+'KB | Total (with lazy chunks): '+ak+'KB');if(crit>200*1024){console.error('FAIL: critical bundle exceeds 200KB budget');process.exit(1);}else{console.log('PASS: critical bundle within 200KB budget');}\"",
    "validate:kernel-content": "node scripts/validate-kernel-content.js",
    "typecheck:kernel": "tsc --noEmit -p js/kernel/tsconfig.kernel.json",
    "playtest:kernel": "node scripts/playtest-kernel-sim.js",
    "ci": "npm run lint && npm run lint:color-debt && npm run typecheck && npm run typecheck:kernel && npm run validate:kernel-content && npm run playtest:kernel && npm run test",
    "smoke:dist": "bash scripts/smoke-dist.sh dist",
    "smoke:src": "bash scripts/smoke-dist.sh src",
    "test:e2e:critical": "playwright test e2e/smoke.spec.js e2e/progression-tier.spec.js"
  },
  "keywords": [
    "idle",
    "game",
    "incremental"
  ],
  "engines": {
    "node": "^18.14.0 || ^20.0.0 || ^22.0.0 || >=24.0.0"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.60.0",
    "axe-core": "^4.12.0",
    "esbuild": "^0.28.0",
    "eslint": "^10.4.1",
    "fake-indexeddb": "^6.2.5",
    "http-server": "^14.1.1",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1",
    "jsdoc": "^4.0.5",
    "prettier": "^3.6.2",
    "sharp": "^0.35.0",
    "terser": "^5.51.2",
    "typescript": "^7.0.0"
  }
}
